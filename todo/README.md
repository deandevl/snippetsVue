To run this sample, enter one of the following scripts as defined in `package.json` in the root directory.

For development:

```
npm run parcel-devel
```

For production:

```
npm run parcel-prod-optimize
```

